{
  "name": "fso",
  "version": "1.0.0",
  "description": "Backend of FullStackOpen's course (part III)",
  "main": "index.js",
  "scripts": {
    "dev": "npx nodemon listener.js",
    "curl": "curl localhost:9001/notes/api/3 && curl localhost:9001/phonebook/api/3",
    "pretty": "npx prettier --write *.js",
    "lint": "npx eslint ."
  },
  "author": "dj-mc",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  }
}
