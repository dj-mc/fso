{
  "name": "fso",
  "version": "1.0.0",
  "description": "Backend of FullStackOpen's course (part III)",
  "main": "index.js",
  "scripts": {
    "dev": "npx nodemon listener.js",
    "debug": "node --inspect listener.js",
    "lint": "npx eslint ./*.js",
    "pretty": "npx prettier --write ./*.js",
    "check": "npm run lint && npm run pretty",
    "clean": "rm -rf dist static index.html",
    "build:ui": "npm run clean && cd ../esb-r && npm run build && npm run dist",
    "deploy": "git push heroku HEAD:master"
  },
  "author": "dj-mc",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.5.1",
    "morgan": "^1.10.0",
    "serve-favicon": "^2.5.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.1"
  }
}
